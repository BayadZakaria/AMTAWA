sed -i 's/         setBudget(userData.daily_budget_mad);/         const today = new Date().toISOString().split('\''T'\'')[0];\n         const lastLoginKey = `last_login_${user.id}`;\n         const lastLogin = localStorage.getItem(lastLoginKey);\n         if (lastLogin !== today) {\n           userData.tokens = Math.max(userData.tokens || 0, 50);\n           await supabase.from('\''users'\'').update({ tokens: userData.tokens }).eq('\''id'\'', user.id);\n           localStorage.setItem(lastLoginKey, today);\n         }\n         setBudget(userData.daily_budget_mad);/g' src/App.tsx
